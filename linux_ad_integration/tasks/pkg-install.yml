---
- name: Upgrade all packages to the latest version
  become_user: root
  apt:
    name: "*"
    state: latest
    update_cache: yes
    force_apt_get: True

- name: Install all required packages
  become_user: root
  apt:
    name: "{{ packages }}"
  vars:
    packages:
      "{{ newpackages }}"
